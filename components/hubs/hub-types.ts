export type HubRecord = { id: string; publicId?: string; name: string; address: string; status?: string; stateId?: string; cityId?: string; stateName?: string; cityName?: string; state?: { publicId?: string; name?: string } | null; city?: { publicId?: string; name?: string } | null; marketIds?: string[]; staffIds?: string[]; markets?: Array<{ id?: string; publicId?: string; name: string; address?: string; status?: string }>; staff?: Array<{ id?: string; publicId?: string; firstName?: string; lastName?: string; email?: string }>; capacity?: { dailyPackages?: number; storageSlots?: number }; contact?: { name?: string; phone?: string; email?: string }; operatingHours?: Record<string, unknown>; coordinates?: { lat?: number; lng?: number }; createdAt?: string; updatedAt?: string };
export type HubCollection<T> = { data: T[]; total?: number };
export type HubLookup = { id?: string; publicId?: string; name: string; stateId?: string; operationsEnabled?: boolean };
export const hubId = (record?: { id?: string; publicId?: string } | null) => record?.publicId || record?.id || "";
