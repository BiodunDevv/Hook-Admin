import { AppTabBarShell } from "@/components/platform/AppTabBarShell";
export default function RunnerLayout({ children }: { children: React.ReactNode }) { return <AppTabBarShell type="runner">{children}</AppTabBarShell>; }
