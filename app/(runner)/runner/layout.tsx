import { PortalShell } from "@/components/platform/PortalShell";
export default function RunnerLayout({ children }: { children: React.ReactNode }) { return <PortalShell type="runner">{children}</PortalShell>; }
